{
  "name": "refurb-zone-redesign",
  "version": "1.0.0",
  "private": true,
  "description": "Redesign Refurb.zone: katalog dílů, detail produktu a servisní služby s JSON daty.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "start": "node scripts/serve.mjs",
    "build": "node scripts/build.mjs",
    "preview": "node scripts/serve.mjs --dist",
    "check": "node scripts/check.mjs",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "3.6.2"
  }
}
